load("//tools/qemu:config.bzl", "qemu_launch_config")

package(default_visibility = ["//visibility:public"])

exports_files(glob(["*.bzl", "*.star", "*.sh", "*.key", "*.keep", "keys/*"]))

qemu_launch_config(name = "launch_config", src = "launch.prototxt")
