load("@rules_rust//rust:defs.bzl", "rust_binary")
package(default_visibility = ["//visibility:public"])

rust_binary(
    name = "cldr_data",
    srcs = ["src/main.rs"],
    edition = "2024",
    deps = [
        "@locale_crates//:icu",
        "@locale_crates//:icu_provider_export",
        "@locale_crates//:icu_provider_source",
    ],
)
