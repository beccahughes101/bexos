package(default_visibility = ["//visibility:public"])

[
    sh_binary(name = name, srcs = ["bin/" + name])
    for name in ["bex_archive", "bexos_assembly", "config_compiler", "fidlc", "manifest_stamp"]
]

filegroup(name = "files", srcs = ["BUILD.bazel"])
